class Solution:
    def reverseString(self, s: List[str]) -> None:
        x=0
        y=len(s)-1
        while(x<y):
            s[x],s[y]=s[y],s[x]
            x=x+1
            y=y-1
        c=""
        for i in s:
            c=c+i
        return c    
